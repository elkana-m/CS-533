# Measures Converter

Flutter app that converts metric and imperial units (length and mass).

```bash
flutter pub get
flutter run
```

Enter a value, pick **From** and **To** units, then tap **Convert**.
To add a unit, append a `MeasureUnit` in `lib/data/unit_catalog.dart`.
